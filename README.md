# First readme notebook

This its one of my first notes from this classes
focusing on learning git codes

## Git COMNANDS-BASH

For starting a new repository on git from github there is a way that can show you many ways to do stuff on git, for example some of those commands could be:

---

git init

You can use this for starting a new repository, this will chose the source you´re in for start reading and creating the master file so you can start creating branches from your file

```
